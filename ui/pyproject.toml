[project]
name = "stock-tracker-ui"
version = "0.1.0"
description = "Streamlit UI for stock tracking"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit",
    "requests",
    "python-dotenv",
    "pandas",
]
