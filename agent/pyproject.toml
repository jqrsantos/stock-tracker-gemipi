[project]
name = "stock-tracker-agent"
version = "0.1.0"
description = "Notification agent for stock tracker"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "python-dotenv",
    "markdown",
]
