[project]
name = "stock-tracker-listener"
version = "0.1.0"
description = "Interactive Telegram bot for ad-hoc research"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot",
    "requests",
    "python-dotenv",
    "yfinance",
]
